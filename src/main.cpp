#include <iostream>
#include <vector>
#include <math.h>

//Comentar linea: ctrl-shift-c, descomentar: ctrl-shift-x

//(Por ahora) Todo el código se encuentra en el mismo archivo
// para facilitar lectura/localizacion del código

//Código de Input/Output (IO)
//Fin código IO

//Código de Fuerza Bruta (FB)

//Generador de conjunto partes
std::vector<std::vector<int> > conjuntoPartes(std::vector<int> conjuntoInicial){
    std::vector<std::vector<int> > conjuntoFinal;
    unsigned long contador;
    unsigned long tamFinal = pow(2, conjuntoInicial.size())

    for(contador = 0; contador <= tamFinal; contador++){
        //
    }

    return conjuntoFinal;
}
//

//Fin código FB

//Código de Backtracking (BT)
//Fin código BT

//Código de Programación Dinámica (PD)
//Fin código PD

int main()
{
    std::cout << "Hello world!" << std::endl;
    return 0;
}
